# Copy this file to .env and fill in your database credentials
# Do NOT commit the .env file with real credentials to source control.

MYSQLHOST=localhost
MYSQLPORT=3306
MYSQLUSER=your_user
MYSQLPASSWORD=your_password
MYSQLDATABASE=your_database

# Optional: port for the Express server
PORT=8080
